import { Box, Typography } from '@mui/material';

const PanelScoring = () => (
  <Box sx={{ p: 3 }}>
    <Typography variant="h5">Panel Scoring</Typography>
    <Typography color="text.secondary" sx={{ mt: 1 }}>Coming soon — this feature is under development.</Typography>
  </Box>
);

export default PanelScoring;
